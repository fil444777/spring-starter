db:
  username: postgres
  password: aezakmi
  url: jdbc:postgresql://localhost:5432/db-hibernate
  hosts: localhost,127.0.0.1
  properties:
   first: 1
   last: 2
   key: value
  pool:
    size: 10
    timeout: 20
  pools:
    - size: 10
      timeout: 20
    - size: 10
      timeout: 20
    - size: 10
      timeout: 20

logging:
  level:
    root: WARN
    spring.database.repository.pool: INFO
  file:
    name: by.javaguru
    path: /


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springdata
    username: postgres
    password: aezakmi
    driver-class-name: org.postgresql.Driver
  jpa:
    properties.hibernate:
      show_sql: true
      format_sql: true
      hdm2ddl.auto: validate